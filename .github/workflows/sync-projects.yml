# Retired. See sync-repos.yml. Safe to delete.
name: Auto-Sync Projects (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Project data lives in index.html."
