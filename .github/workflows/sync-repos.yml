# Retired. The site no longer reads repos-data.json or projects.json;
# project cards are written by hand in index.html. This file is kept
# only so the scheduled job stops; it can be deleted together with the
# generated JSON files.
name: Auto-Sync GitHub Repos (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Project data lives in index.html."
